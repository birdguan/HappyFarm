<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.birdguan.cownet.BaseInfoActivity"
    android:gravity="center_horizontal">

    <com.baidu.mapapi.map.MapView
        android:id="@+id/map_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clickable="true" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginTop="10dp"
        android:gravity="center_horizontal">
        <EditText
            android:id="@+id/editText_locationQuery"
            android:layout_width="200dp"
            android:layout_height="40dp"
            android:imeActionLabel="确定"
            android:imeOptions="actionNext"
            android:inputType="number"
            android:maxLength="11"
            android:padding="8dp"
            android:text=""
            android:selectAllOnFocus="false" />

        <Button
            android:id="@+id/button_locationQuery"
            android:layout_width="90dp"
            android:layout_height="40dp"
            android:layout_marginLeft="20dp"
            android:layout_alignTop="@id/map_view"
            android:layout_marginBottom="10dp"
            android:layout_alignRight="@id/map_view"
            android:layout_marginRight="10dp"
            android:textSize="18sp"
            android:background="#88ffffff"
            android:text="查询" />
    </LinearLayout>

    <Button
        android:id="@+id/btnPos"
        android:layout_width="90dp"
        android:layout_height="35dp"
        android:layout_alignBottom="@id/map_view"
        android:layout_marginBottom="10dp"
        android:layout_alignRight="@id/map_view"
        android:layout_marginRight="80dp"
        android:background="#eeffffff"
        android:text="我的位置" />

</RelativeLayout>
